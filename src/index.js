import React from 'react';
import ReactDOM from 'react-dom';
import TicketToRide from './App'; // 确保这里的路径指向包含 TicketToRide 组件的文件

ReactDOM.render(<TicketToRide />, document.getElementById('contents'));